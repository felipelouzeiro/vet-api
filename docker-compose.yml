version: '3.1'

services:
  db:
    image: postgres
    restart: always
    ports:
      - 5432:5432
    environment:
      POSTGRES_USER: your_username # example
      POSTGRES_PASSWORD: your_password # example
      POSTGRES_DB: vet_database
